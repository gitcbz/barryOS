# Build outputs — regenerate with `make all`.  Nothing here is source, and the
# ISO alone is 36 MB, which is worth not committing on every round.
/build/
kernel/target/

# Editor and OS noise
*.swp
*~
.DS_Store

# Python bytecode from the build-time generators
__pycache__/
